{
  "name": "maxy-chat-backend",
  "version": "2.0.0",
  "description": "MAXY Chat Backend API",
  "main": "server.py",
  "scripts": {
    "start": "gunicorn server:app -w 4 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000",
    "dev": "uvicorn server:app --reload --host 0.0.0.0 --port 8000",
    "test": "pytest"
  },
  "engines": {
    "node": "18.x"
  }
}
